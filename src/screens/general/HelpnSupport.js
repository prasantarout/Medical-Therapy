import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const HelpnSupport = () => {
  return (
    <View>
      <Text>HelpnSupport</Text>
    </View>
  );
};

export default HelpnSupport;

const styles = StyleSheet.create({});
